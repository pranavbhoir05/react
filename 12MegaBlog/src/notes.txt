vid no 19 :

*make sure to create .env outside of src
*mkae sure to add .env on gitignore

#how to access env file?
to access there are duffrent ways to access file for react,backend,frontend everyone of them there are diffrent ways to access

#if someone craetapp using createreactapp then its compulsary to start variable in env with  REACT_APP
#doc : These environment variables will be defined for you on process.env. For example, having an environment variable named REACT_APP_NOT_SECRET_CODE will be exposed in your JS as process.env.REACT_APP_NOT_SECRET_CODE.

*but we have create this app by using vite so it will be using vite: 
To prevent accidentally leaking env variables to the client, only variables prefixed with VITE_ are exposed to your Vite-processed code. e.g. for the following env variables:
VITE_SOME_KEY=123
DB_PASSWORD=foobar

Only VITE_SOME_KEY will be exposed as import.meta.env.VITE_SOME_KEY to your client source code, but DB_PASSWORD will not.
console.log(import.meta.env.VITE_SOME_KEY) // "123"
console.log(import.meta.env.DB_PASSWORD) // undefined

*after appwrite url we gonna have to add morw things in (env)
*VITE_APPWRITE_PROJECT_ID , 
VITE_APPWRITE_DATABASE ,
VITE_APPWRITE_COLLECTION_ID
VITE_APPWRITE_BUCKET_ID

*after that go to appwrite and crate new project and copy projectid and paste in env
*also create database in appwrite and copy paste id in env
*in created db creat table(collection) and copy paste to env
*in that table create  collums for id ,content, featuredimage, status, userid and also in indexes create indeax and choose status and key
*go to storage and create bucket and copy past id in env ,also make changes in permisssion

#insted of writing (import.meta.env.VITE_APPWRITE_URL )all the time we;ll create separate folder and file to handle this env  (to make it shortcut)
* so creat folder and file with name conf and in env and create separate method for each operation like things:
 const conf = {
    appwrite: String(import.meta.env.VITE_APPWRITE_URL),
 }

 ##########################################################################
  video no 20 :  Build authentication service with appwrite
we gonna create services (class): but why? 
   cause even if tommorow i want remove my authentication from appwrite, then still application should be manage to run, so we;ll create services
   #read txt file i hvae created named: what is a services.txt
   #it doesnt matter from which server we talk
   #go through documentaion of appwrite, appwrite will, all things we dont need worry about encruption and all

   *remember go through appwrite documentaion from creating clients to mails and all
   *create folder name appwrite, cause all services will be related appwrite
   *in that folder create file name auth.js
   *as we gonna need databaseid, and all ids so import (conf) as we have created 
   #we can also copypast code but we;ll be using class

   *create class(authservice) and export it (as we have created class now anyone who use this class needs to creaete object)
   #insted if we create object and directly export, and can be apply methods on that object
   *just cause we use new keyword so we are also calling constructor

   #The result: the application interacts with AuthService instead of interacting with Appwrite directly. This creates an abstraction layer, improves maintainability, reduces vendor dependency, and makes the system easier to modify or scale.

in this video we create account, also creaete session,delete and getuser and all..
   #############################################################################

   video no 21 : Appwrite database, file upload and custom queries

   *as we hvea created appwrite service(auth.js), same creat another service name(config.js)
   *
   *
   *
  ####################################################################################
   vid no 22:   configure redux toolkit in big projects    

     *create store (folder and file) then create folder for reducers
     *then configure reduser and export
     *then creaete components for header and footer and also create separate index file and import both file in it and export
     *go in app.jsx cause most the work is gonna be handling this file
     #cause whenever the app loads ,we gonna have to see user is loded in or not, and this whole thing we'll see in this state
     #if user is loged in then we'll show few things , if not then we'll give em outlet that we'll not show anypost to them 
     # we create state cause when we fetch data, there can be some chances of delay in network request,so wheneverwe wants to request from (database or network) we create loading state
     #creating loading state is always good practice, so we can perform conditionly render by if and else, if loading is true, we'll show loading icon if not then will show data

   * *create state (loading) then also create dispatch
      *now i need service(auth) now import authservice
      *now whenever this app loads, then take useeffect and ask that (searvice) its login ot not
      *use useeffectand call authservice and getcurrentuser,ifm current user found .then
      *in that .then we get callback, put any variable in parameter and call dispatch method and dispatch (login) from authslice 
      #when login ,user status will changes itself but userdata needs to be dispatch
      *so before dispatching userdata,add if statement if user dont exist
      #Key rule: dispatch(login(value)) → value becomes action.payload inside the reducer.
      *but the loading is still true ,if all the checking is done then call finally and call setloading false
      *now return in condional rendering, directly add if statement if (!loading), then pass header and footer components
      *we'wont add outlet yet, cause didnt created
      *setup (provider) in main.jsx, import provider and store.js in main.jsx
      *and wrap app in provider
      *also creat pages folder in src 
      
############################################################################3
vid 23:  Production grade react components 

* created container folder and file in components
*container is just box ,we define styling properties (height,width) in container
*add classes in container,but which classes , i want width full, so what the benefits now is (if i want width 80% in all components, then what ill do is,i ll shift the container and the work is done)
*now go to footer and add css from git, and import link from router aslo create logo.jsx in components folder and import in footer

#go to header(header is optional,we dont wanna show all value at once,and who do we waNT to show login logout we gonna have to judge it, on the besis of its login or logout )

*create file in header name logoutbtn.jsx,but during logout we gonna take (action, we gonnna have to dispatch), so to logout we gonna have to bring slice from store too
*import dispatch and also import authService(cause our logout is in it)
*we also gonna have bring individual service of logout, so import it from features
*in logoutbtn add dispatch method

#just cause its gonna be btn then create logout handler in it
*create method for btnhandler then call logout() from authservice, and just cause if u see in appwrite in auth, logout is promice, so wheneverwe logout we get promice
*call the logout method from the authService to log the user out and then dispatch the logout action to update the Redux state
*create btn in return

#we'll add all the links in header,we'll conditionally render if we wanna show logout or not (we'll check user is logged in or not)
*in handler import container if u want, first import (container,LogoutBtn,logo in index.js)
*now in header import Container,Logo,LogoutBtn from index file and also useNavigate,useSelector,Link

*we gonna have to check if its authented or not , creaet state fore this
 *create navigation same as create usedispatch
*navItems is an array of objects that stores navigation data. create navitems,advantage:Store menu information in one place instead of writing many <li> elements.
# Data (navItems array)
        ↓
Loop (map)
        ↓
UI generated automatically

#now create objects and active will depends on authstatus, and we'll disppley all active things by applying consitional rendering ,if user is login then why to show him login and signin button
*in header add <header> in that add container, now all the navigation bar are in container
#in this container we;ll take nav component and will add logo then will loop to ui

*take nav first add div in it(will add logo in this div), also add link tag and inside of it add Logo(which we already created )
*in nav take another div for ul and few css flex and ml-auto

*now start javascript apply map()on navItems and check if item are active , if yes then add li
*if things are getting repeated then add (key) in li
*now we need button(all navigation will happen in this button) name will be same as key item.name
*when applying onclick on button , callback with navigate which we have created and pass (item.path) as u named it, and makesure to add class
* If user is authenticated,then show Logout button
##############################################

*now Create one reusable button component used across the application.
#Benefits: 1. Reusability
   Same button used across pages (login, submit, nav, etc.).

2. Consistent styling
   All buttons share identical design.

3. Easier maintenance
   Change style once → updates everywhere.

4. Cleaner components
   Removes repeated Tailwind classes.

5. Extensibility
   Props can control color, size, or behavior.

Definition:
A common Button component is a reusable UI component created once and used in multiple places to maintain consistent styling and reduce repeated code.

#########################################################################33
*create button.jsx in component folder
*as parameter pass{children and add as u wants to change} we also add classname empty cause by doing this we can modify and extendable classes
*create button in button and pass {children} (nameany, most people call this children, so never mind)
*also pass ...props //by adding ...props now we can pass any additional props to the button element when we use the Button component, such as onClick handlers, disabled state, etc. This makes the Button component more flexible and reusable in different contexts throughout our application.

##########################################3
#same syntax we;ll be repeating in input button, there is also new hook call forwardref()
#i have created notes on this intxt
*create input.jsx ion component,rfce then also import useid also store id in variable
*as we did in button we gonna add parameter in{}, but this time hold function in forwardref and hold in variable
*give all input element in parameter (lebal, classname,type,...prps) but we'll also pass ref(reference) 

#ref is a reference that allows a parent component to directly access and control a DOM element inside a child component.
flow : 
Parent component
     ↓
useRef() creates ref
     ↓
ref passed to Input component
     ↓
forwardRef allows Input to receive it
     ↓
ref attached to <input>

#################################
LoginForm → Parent
Input     → Child

########################################################################################

*in return, start your javascript and check if leble is present and add lebel ,htmnlfor and classname
*for input add type to type, as we;have created in parameter and also provide classname , ...props ,ref and id
#i have created file name ref.txt in notes just read it 
#now unique id attach to input element and label element, so they are properly associated

###########################################################################################3

vid : 24 How to use React hook form in production

#as we know we have (dropdown button)select button in my projecet ,which will show active and inactive status,so 
*create new file select.jsx in component,the crate function
#for selecting active deactive we need options , then give para options
*and create select button same as we did input.jsx

#as we know we were getting cards, whereever we click on card,we get to read its artical, so that card is component itself, lets create card
*create component card and rfce
#we also gonna need information,so 
* import services from appwrite/config,appwrite searvice will add query on it, the reason we are importing from it , IF WE HAD ALL THINGS IN STATES, WE WOULD,VE USE REDUX, AND TOOK INFO FROM STATE 
*also import link from react router dom
#we gonna have to passs props,which we will get from appwrite when we perform  query
*add ($id,title,featuredImage) $id is the unique document ID generated by Appwrite
#we havent define routers yet,so dont get confused in <Link>
*in return creat Link component and create 2 div component, one for Card container styling and 2nd for img wrapper
*now use img tag ,in src add getFilePreview from appwriteService and provide (featuredImage)

#dont get confused over $id and featureimage:
# $id : Represents the unique ID of the post document in the database., purpose: Navigate to the specific post page.
Example route → /post/post123
# featuredImage : Represents the file ID stored in Appwrite Storage.,purpose:Fetch image URL from storage so the image can be displayed.

*but the questionj is why using getFilePreview instead getPost()
#// getPost() does not return the image itself.
// It returns post data, and the image field is only a file ID.
#//Why getFilePreview() is needed
// Storage converts the file ID into a real image URL.
*also add tital in h2
______________________________________________________
react hook form
#now its time to create login component not login page,
#login component is Reusable component that renders the login form UI and handles authentication logic.

*create Login.jsx in component and import usestate
*import link and useNavigate, cause after login we'll send to somewhere
*we also gonna need login from authslice ,so import from ../features/authSlice'
*import {Button, Input ,Logo}which we created,then usedispatch and AuthService and react hook form

*now hold in variable : useNavigate,usedispatch
*also add reacthook syntax and go thruoght documentaion of react hook form,in this syntax dont get ful  const {register, handleSubmit} = useForm()  , this register doesnt mean register
*create usestate to display errors  
*now create method (login) insted og naming submit, it will cause confusion,
*in that login method use async method,whenever sets login call setError("") method imidiatly to empty errors
*use trycatch : try authenticating the user with the provided data (email and password) and hold in variable and await         
* add condition if authentication is successful, retrieve the current user's data using the getCurrentUser method of the authService instance , hold in variable and await
* in same if again add condition if user data is successfully retrieved, dispatch the authLogin action with the user data to update the authentication state in the Redux store
* and navigate('/')  //navigate to the home page after successful login
*catch (error) and setError(error.message)
*in return add your css ,and then form


#while creating form we'll go through syntax(precisly)
#when we bring from from react hook,we get const {register, handleSubmit} = useForm()
* always add {handleSubmit} in onSubmit , and just cause handleSubmit is method(), then pass the (login) which we created
#always remember handleSubmit is keyword , is came from form , be aware with naming
# add classes for form and create div for input component
*add input component we have created earlier in components and 
*pass things in input like lable,placeholder and type
*then start ur javascript here, sprade (...register) what it does is extracts the necessary values and functions from the register object and passes them as props to the Input component,
*always kepp unique value in register, add register("email") , // define field name → will appear as { email: value }
*add required true and
*also create object (validate) in this object again creat obj(matchPatern) nameany and pass function(value)it 
*in that function add regex (from regexr.com for email) and after extracting value from regexr
* .test(value) and also add || (or)  ,If fails → returns error string and close the input tag

*now do same thing to create method for password in form
*now add button as we created and add type and classname

#now create same form for sign up
*create signup.jsx in component and follow all steps as login.

_________________________________________________________
#we gonna create authlayout which will Protect routes based on authentication.
*Purpose of the entire Protected code is To control who can view a page based on login status.
#User hits route
     ↓
AuthLayout checks authStatus
     ↓
Allowed → render children
Blocked → redirect

*create Authlayout.jsx in components,this will be our protected container   
#we'll conditionally render if render to that children or not
*pass props {children and authentication} and authentication true ,we have give truw, but someone mayhave given false, so on that bases we;ll check value and update
*for clear understanding i have created text file name:
(protected.2.txt)


#realtime editor is yet to create

###########################################################################################
vid no 25:  Adding form and slug values

*about rte i have save chatgpt responce  in txt notes file